import { PizzasFiltersPipe } from './pizzas-filters.pipe';

describe('PizzasFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new PizzasFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
